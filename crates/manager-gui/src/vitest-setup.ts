// Auto-loaded before every test file (see `vite.config.ts`'s
// `test.setupFiles`). Registers `@testing-library/jest-dom`'s matchers
// (`toBeInTheDocument`, ...) with vitest's own `expect`, and gives TypeScript
// their types.
import "@testing-library/jest-dom/vitest";
